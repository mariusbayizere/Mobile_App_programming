name: mobile_programming
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  provider: ^6.0.0
  shared_preferences: ^2.0.15
  image_picker: ^0.8.4+8
  uuid: ^3.0.6
  image_picker_for_web: ^2.2.0
  sqflite: ^2.0.0+4
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
